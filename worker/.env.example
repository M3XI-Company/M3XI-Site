# Copy to worker/.env and fill in. NEVER commit worker/.env (it is gitignored).
# Supabase dashboard -> Project Settings -> API
SUPABASE_URL=https://tnlcuptfldwxtxajudoq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Optional
WORKER_NAME=            # defaults to this machine's hostname
WORKER_KINDS=video      # comma separated: video,image
MODEL=Wan-AI/Wan2.2-TI2V-5B-Diffusers
BACKEND=diffusers       # diffusers | comfy | mock
# GPU_NAME=RTX 4090     # label shown in the workers table (auto-detected otherwise)
# COMFY_URL=http://127.0.0.1:8188
# COMFY_WORKFLOW=C:\path\to\workflow_api.json
